---
import EyeOpenIcon from "@/icons/svg/EyeOpenIcon.astro";
import EyeClosedIcon from "@/icons/svg/EyeClosedIcon.astro";
---
<div
  x-data="{ isVisible: $persist(true) }"
  class="border rounded-lg shadow-sm bg-cl-ui_strong"
>
  <div
    class="flex justify-between cursor-pointer p-4"
    @click="isVisible = !isVisible"
  >
    <h2 class="text-xl font-bold">About Me</h2>
    <button
      aria-label="Toggle about me section"
      class="text-cl-text_weak cursor-pointer"
    >
      <!-- Eye Open Icon (when content is visible) -->
      <EyeOpenIcon x-show="isVisible" />
      <!-- Eye Closed Icon (when content is hidden) -->
      <EyeClosedIcon x-show="!isVisible" />
    </button>
  </div>
  <div
    x-show="isVisible"
    class="space-y-3 p-4 pt-0"
  >
    <p>Hello! I'm a passionate blogger and developer who loves to share insights about technology, programming, and web development.</p>
    <p>This blog is my platform to document my journey, share what I've learned, and hopefully help others along the way.</p>
  </div>
</div>